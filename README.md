# Argus

Argus is a Hermes-native test runner for React Native. It executes your tests on the standalone Hermes engine — the real production VM used by React Native — giving engine fidelity at unit-test cost, with no device or emulator required.

**Status: Draft v0.1**

See [docs/SPEC.md](docs/SPEC.md) for the full architecture and rationale.
