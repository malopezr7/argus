node_modules
# Test fixtures simulating a dependency tree must be tracked
!packages/adapter-esbuild/src/__tests__/fixtures/node_modules/
dist
.codegraph/
*.tsbuildinfo
.DS_Store
/spike
coverage
.hermes/
*.argus-bundle.js

# Tooling artifacts (gentle-ai skill registry, etc.)
.atl/

# Specs / SDD artifacts — kept LOCAL, never committed (per project preference).
# Anchored to the repo root so the docs site's own content directory
# (website/src/content/docs) stays tracked.
/docs/
/openspec/

# Agent handoff / working context — local only, never committed
AGENTS.md

# JetBrains IDEs
.idea/
