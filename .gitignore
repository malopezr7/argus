node_modules
dist
*.tsbuildinfo
.DS_Store
/spike
coverage
.hermes/
*.argus-bundle.js

# Tooling artifacts (gentle-ai skill registry, etc.)
.atl/

# Specs / SDD artifacts — kept LOCAL, never committed (per project preference)
docs/
openspec/

# Agent handoff / working context — local only, never committed
AGENTS.md
