import { describe, expect, it } from 'vitest';

describe('core smoke test', () => {
  it('arithmetic works (placeholder until real domain helpers exist)', () => {
    expect(1 + 1).toBe(2);
  });
});
