{
  // Written the way real @tsconfig/* presets are: JSONC, not JSON.
  "compilerOptions": {
    /* the settings that decide emit live here, not in the project */
    "experimentalDecorators": true,
    "useDefineForClassFields": false,
  }
}
